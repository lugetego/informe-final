{% extends 'base.html.twig' %}


{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.19/css/jquery.dataTables.min.css"/>
{% endblock %}


{% block body %}
    <h1>Artículos 2015</h1>

    <table id="pub15" class="table table-striped table-bordered" >
        <thead>
        <tr>
            <th>Titulo</th>
            <th>Tipo</th>
            <th>Autor</th>
            <th>Tipo de autor</th>
            <th>Revista</th>
            <th>Año</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for publicacion in publicacions15 %}
            <tr>
                <td>
                    {% if publicacion.url is null %}
                        {{ publicacion.titulo }}
                    {% else %}
                        <a href="{{ publicacion.url }}" target="_blank">{{ publicacion.titulo }}</a>
                    {% endif %}
                </td>
                <td>{{ publicacion.tipo }}</td>
                <td>{{ publicacion.autor }}</td>
                <td>{{ publicacion.tipoautor }}</td>
                <td>{{ publicacion.journal }}</td>
                <td>{{ publicacion.anio }}</td>
                <td>
                    <ul>
                        {#                        <li>#}
                        {#                            <a href="{{ path('publicaciones_show', { 'id': publicacion.id }) }}">show</a>#}
                        {#                        </li>#}
                        <li>
                            <a href="{{ path('publicaciones_edit', { 'id': publicacion.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <h1>Artículos 2016</h1>

    <table id="pub16" class="table table-striped table-bordered" >
        <thead>
        <tr>
            <th>Titulo</th>
            <th>Tipo</th>
            <th>Autor</th>
            <th>Tipo de autor</th>
            <th>Revista</th>
            <th>Año</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for publicacion in publicacions16 %}
            <tr>
                <td>
                    {% if publicacion.url is null %}
                        {{ publicacion.titulo }}
                    {% else %}
                        <a href="{{ publicacion.url }}" target="_blank">{{ publicacion.titulo }}</a>
                    {% endif %}
                </td>
                <td>{{ publicacion.tipo }}</td>
                <td>{{ publicacion.autor }}</td>
                <td>{{ publicacion.tipoautor }}</td>
                <td>{{ publicacion.journal }}</td>
                <td>{{ publicacion.anio }}</td>
                <td>
                    {#                    <ul>#}
                    {#                        <li>#}
                    {#                            <a href="{{ path('publicaciones_show', { 'id': publicacion.id }) }}">show</a>#}
                    {#                        </li>#}
                    <li>
                        <a href="{{ path('publicaciones_edit', { 'id': publicacion.id }) }}">edit</a>
                    </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <ul>
        <li>
            <a href="{{ path('publicaciones_new') }}">Create a new publicacion</a>
        </li>
    </ul>

    <h1>Artículos 2017</h1>

    <table id="pub17" class="table table-striped table-bordered" >
        <thead>
        <tr>
            <th>Titulo</th>
            <th>Tipo</th>
            <th>Autor</th>
            <th>Tipo de autor</th>
            <th>Revista</th>
            <th>Año</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for publicacion in publicacions17 %}
            <tr>
                <td>
                    {% if publicacion.url is null %}
                        {{ publicacion.titulo }}
                    {% else %}
                        <a href="{{ publicacion.url }}" target="_blank">{{ publicacion.titulo }}</a>
                    {% endif %}
                </td>
                <td>{{ publicacion.tipo }}</td>
                <td>{{ publicacion.autor }}</td>
                <td>{{ publicacion.tipoautor }}</td>
                <td>{{ publicacion.journal }}</td>
                <td>{{ publicacion.anio }}</td>
                <td>
                    {#                    <ul>#}
                    {#                        <li>#}
                    {#                            <a href="{{ path('publicaciones_show', { 'id': publicacion.id }) }}">show</a>#}
                    {#                        </li>#}
                    <li>
                        <a href="{{ path('publicaciones_edit', { 'id': publicacion.id }) }}">edit</a>
                    </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <ul>
        <li>
            <a href="{{ path('publicaciones_new') }}">Create a new publicacion</a>
        </li>
    </ul>

    <h1>Artículos 2018</h1>

    <table id="pub18" class="table table-striped table-bordered" >
        <thead>
        <tr>
            <th>Titulo</th>
            <th>Tipo</th>
            <th>Autor</th>
            <th>Tipo de autor</th>
            <th>Revista</th>
            <th>Año</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for publicacion in publicacions18 %}
            <tr>
                <td>
                    {% if publicacion.url is null %}
                        {{ publicacion.titulo }}
                    {% else %}
                        <a href="{{ publicacion.url }}" target="_blank">{{ publicacion.titulo }}</a>
                    {% endif %}
                </td>
                <td>{{ publicacion.tipo }}</td>
                <td>{{ publicacion.autor }}</td>
                <td>{{ publicacion.tipoautor }}</td>
                <td>{{ publicacion.journal }}</td>
                <td>{{ publicacion.anio }}</td>
                <td>
                    {#                    <ul>#}
                    {#                        <li>#}
                    {#                            <a href="{{ path('publicaciones_show', { 'id': publicacion.id }) }}">show</a>#}
                    {#                        </li>#}
                    <li>
                        <a href="{{ path('publicaciones_edit', { 'id': publicacion.id }) }}">edit</a>
                    </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <ul>
        <li>
            <a href="{{ path('publicaciones_new') }}">Create a new publicacion</a>
        </li>
    </ul>

    <h1>Tesis</h1>

    <table id="tesis" class="table table-striped table-bordered" >
        <thead>
        <tr>
            <th>Titulo</th>
            <th>Autor</th>
            <th>Institucion</th>
            <th>Grado</th>
            <th>Anio</th>
            <th>Asesor</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for tesi in teses %}
            <tr>
                <td>
                    {% if tesi.url is null %}
                        {{ tesi.titulo }}
                    {% else %}
                        <a href="{{ tesi.url }}" target="_blank">{{ tesi.titulo }}</a>
                    {% endif %}
                </td>
                <td>{{ tesi.autor }}</td>
                <td>{{ tesi.institucion }}</td>
                <td>{{ tesi.grado }}</td>
                <td>{{ tesi.anio }}</td>
                <td>{{ tesi.asesor }}</td>
                <td>
                    <ul>
{#                        <li>#}
{#                            <a href="{{ path('tesis_show', { 'id': tesi.id }) }}">show</a>#}
{#                        </li>#}
                        <li>
                            <a href="{{ path('tesis_edit', { 'id': tesi.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <ul>
        <li>
            <a href="{{ path('tesis_new') }}">Create a new tesis</a>
        </li>
    </ul>


    <h1>Otros list</h1>

    <table id="otros" class="table table-striped table-bordered" >
        <thead>
        <tr>
            <th>Id</th>
            <th>Autor</th>
            <th>Titulo</th>
            <th>Tipo</th>
            <th>Anio</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for otro in otros %}
            <tr>
                <td><a href="{{ path('otros_show', { 'id': otro.id }) }}">{{ otro.id }}</a></td>
                <td>{{ otro.autor }}</td>
                <td>{{ otro.titulo }}</td>
                <td>{{ otro.tipo }}</td>
                <td>{{ otro.anio }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('otros_show', { 'id': otro.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('otros_edit', { 'id': otro.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('otros_new') }}">Create a new otro</a>
        </li>
    </ul>


{% endblock %}


{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.1.32/pdfmake.min.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.1.32/vfs_fonts.js"></script>
    <script type="text/javascript" src="https:////cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js"></script>

    <script>
        $(document).ready(function() {
            $('#pub15').DataTable({


                pageLength: 15,

            } );
        } );

        $(document).ready(function() {
            $('#pub16').DataTable({


                pageLength: 15,

            } );
        } );

        $(document).ready(function() {
            $('#pub17').DataTable({

                pageLength: 15,

            } );
        } );

        $(document).ready(function() {
            $('#pub18').DataTable({

                pageLength: 15,

            } );
        } );


        $(document).ready(function() {
            $('#tesis').DataTable({


                pageLength: 15,

            } );
        } );

        $(document).ready(function() {
            $('#otros').DataTable({


                pageLength: 15,

            } );
        } );
    </script>
{% endblock %}